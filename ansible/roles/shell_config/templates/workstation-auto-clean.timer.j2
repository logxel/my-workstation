# {{ ansible_managed | default('Managed by Ansible (roles/shell_config) — local edits will be overwritten.') }}
[Unit]
Description=Run periodic cleanup for stale files and developer tool caches

[Timer]
OnCalendar=daily
Persistent=true
RandomizedDelaySec=1h
Unit=workstation-auto-clean.service

[Install]
WantedBy=timers.target
